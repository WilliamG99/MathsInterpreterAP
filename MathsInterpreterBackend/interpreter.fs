// interpreter.fs
namespace MathsInterpreterBackend

module Interpreter =
    let add         a b    = a + b
    let minus       a b    = a - b
    let multiply    a b    = a * b
    let divide      a b    = a / b
